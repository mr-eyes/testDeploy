name: Command via SSH

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Execute SSH commmands on remote server
        uses: JimCronqvist/action-ssh@master
        with:
          hosts: 'root@64.227.2.82'
          privateKey: ${{ secrets.SSHKEY }}
          command: ls -lah